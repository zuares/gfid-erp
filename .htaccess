<IfModule mod_rewrite.c>
    RewriteEngine On

    # Kalau URL belum mengarah ke /public, arahkan ke /public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
